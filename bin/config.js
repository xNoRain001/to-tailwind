const config = {
  html: {
    option: '--html <v>',
    descriptor: 'html input'
  },

  css: {
    option: '--css <v>',
    descriptor: 'css input'
  },

  output: {
    option: '--output <v>',
    descriptor: 'output directory'
  },

  inject: {
    option: '--inject <v>',
    descriptor: 'inject tailwind expressions to html'
  }
}

module.exports = config
